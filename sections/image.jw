<section class="basic {{ section.colors ? section.colors }}" id="{{section.id}}">
    <div class="basic-content text-center">
      {% if media %}
        <div class="basic-media">
            <img src="{{ media }}" alt="{{ media.alt }}">
        </div>
      {% endif %}
    </div>
</section>